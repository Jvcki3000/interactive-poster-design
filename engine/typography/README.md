# typography — 排版

**程序化排版**是本 Skill 的核心差异化：文字永远由代码排版，保证准确。

## 计划能力
- `buildTypeScale`：字号阶梯（比例尺，如 1.25）
- `fitText`：文本自适应（自动缩小字号/换行，避免溢出）
- `measure`：文本度量，供 layout 计算
- 支持中文/西文混排、行高与字距规则

## 关键原则
- 标题、日期、Logo、二维码、价格等文字：**绝不交给图像模型**
- 所有文字可在最终 HTML 中被选中、被 SEO、被复制
