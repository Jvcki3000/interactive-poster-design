# animation — 动画系统

让 AI 只需输出**动画规则**（如 `title: { reveal: 'chars' }, image: { zoom: 1.15 }`），由本模块转成实际动画。

## 计划能力
- `eases`：缓动函数库
- `tween`：数值/样式过渡
- `keyframes`：关键帧动画
- `timeline`：时间线编排（逐字出现、依次入场、循环漂浮…）
- 动画规则 schema 与 interaction 配合（hover 触发、点击触发）
