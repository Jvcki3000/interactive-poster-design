# layout — 版式

负责把 Design Spec 的构图意图转成真实版面。

## 计划能力
- `grid`：栅格系统（12 列 / 间距 / 边距）
- `stack`：Z 轴分层（背景 → 主体 → 文字 → 光效，供视差/3D 使用）
- `spacing`：留白 token
- `align`：对齐规则

## 设计原则
- 参考经典构图：瑞士网格、杂志编辑、极简留白
- 每层独立 Z 轴 → 直接支撑 parallax / 3D 效果
